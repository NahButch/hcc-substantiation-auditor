[project]
name = "hcc-eval-harness"
version = "0.1.0"
description = "Evaluation harness for the HCC Substantiation Auditor"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"
